pretest:
  required_columns:
    - participant_id
  completion_mode: all_non_empty

posttest:
  required_columns:
    - participant_id
  completion_mode: all_non_empty

storybook:
  required_columns:
    - participant_id
    - book_completed
  completion_mode: all_non_empty

fully_completed:
  sources:
    - pretest
    - posttest
    - storybook
